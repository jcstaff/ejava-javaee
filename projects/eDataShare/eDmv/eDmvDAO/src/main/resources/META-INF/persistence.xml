<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="http://java.sun.com/xml/ns/persistence"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://java.sun.com/xml/ns/persistence 
        http://java.sun.com/xml/ns/persistence/persistence_1_0.xsd" 
    version="1.0">

    <persistence-unit name="eDmvBO">
        <provider>org.hibernate.ejb.HibernatePersistence</provider>

        <class>ejava.projects.edmv.bo.Person</class>
        <class>ejava.projects.edmv.bo.VehicleRegistration</class>
        
        <properties>
            <property name="hibernate.dialect" 
                value="${hibernate.dialect}"/>
            <property name="hibernate.connection.url"
                value="${jdbc.url}"/>
            <property name="hibernate.connection.driver_class" 
                value="${jdbc.driver}"/>
            <property name="hibernate.connection.password" 
                value="${jdbc.password}"/>
            <property name="hibernate.connection.username" 
                value="${jdbc.user}"/>
            <property name="hibernate.show_sql" value="false"/>            
            <!-- uncomment this to permit Hibernate to create the schema
                 this is a great Java-first technique for creating schema
            <property name="hibernate.hbm2ddl.auto" value="create"/>
            -->
        </properties>
    </persistence-unit>        
    
</persistence>
